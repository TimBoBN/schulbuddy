# SchulBuddy Docker Compose Konfiguration
# Kopiere diese Datei zu .env und passe die Werte an

# === Multi-Architecture Setup ===
# TAG bestimmt welche Image-Version verwendet wird
TAG=latest
# TAG=dev
# TAG=v1.0.0

# PLATFORM erzwingt eine bestimmte Architektur (normalerweise nicht nötig)
# PLATFORM=linux/amd64
# PLATFORM=linux/arm/v7
# PLATFORM=linux/arm64

# === Netzwerk Konfiguration ===
# Port auf dem Host-System
EXTERNAL_PORT=5000
# Port im Container
PORT=5000
HOST=0.0.0.0

# === Sicherheit ===
# WICHTIG: Ändere diesen Schlüssel für Produktion!
SECRET_KEY=your-production-secret-key-change-this-to-something-secure

# === Gunicorn Performance (Multi-Arch optimiert) ===
# auto = automatische Architektur-Erkennung (2 Worker für ARM, 4 für AMD64)
WORKERS=auto
# Oder manuell setzen:
# WORKERS=2   # Für ARM/schwächere Systeme
# WORKERS=4   # Für AMD64/stärkere Systeme

WORKER_CLASS=sync
TIMEOUT=120

# === Session Konfiguration ===
SESSION_TIMEOUT_MINUTES=120
REMEMBER_COOKIE_DAYS=30
LOGIN_TIMEOUT_MINUTES=60
MAX_LOGIN_ATTEMPTS=5
LOGIN_ATTEMPT_TIMEOUT_MINUTES=15

# === Schuljahr Einstellungen ===
CURRENT_SCHOOL_YEAR=2024/25
CURRENT_SEMESTER=1

# === Registry Auswahl ===
# Standardmäßig wird Docker Hub verwendet
# Für GitHub Container Registry, ändere die image-Zeile in docker-compose.yml
