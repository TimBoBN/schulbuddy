# SchulBuddy Multi-Platform Docker Konfiguration
# Optimiert für automatische Architektur-Erkennung

# === Multi-Platform Setup ===
# Verfügbare Tags:
# - dev-multiplatform (Development, alle Architekturen)
# - latest-multiplatform (Production, alle Architekturen) 
# - [version]-multiplatform (Spezifische Version, alle Architekturen)
TAG=dev-multiplatform

# === Netzwerk Konfiguration ===
EXTERNAL_PORT=5000
PORT=5000
HOST=0.0.0.0

# === Sicherheit ===
SECRET_KEY=multiplatform-secret-key-change-this-in-production

# === Performance (automatisch durch entrypoint.sh optimiert) ===
# auto = automatische Architektur-Erkennung:
# - ARM (Raspberry Pi): 2 Worker
# - AMD64 (PC/Server): 4 Worker
WORKERS=auto
WORKER_CLASS=sync
TIMEOUT=120

# === Session Konfiguration ===
SESSION_TIMEOUT_MINUTES=120
REMEMBER_COOKIE_DAYS=30
LOGIN_TIMEOUT_MINUTES=60
MAX_LOGIN_ATTEMPTS=5
LOGIN_ATTEMPT_TIMEOUT_MINUTES=15

# === Schuljahr Einstellungen ===
CURRENT_SCHOOL_YEAR=2024/25
CURRENT_SEMESTER=1

# === Registry Auswahl ===
# Docker Hub (Standard): docker.io/timbobn/schulbuddy:${TAG}
# GHCR (Alternative): ghcr.io/timbobn/schulbuddy:${TAG}
