<<<<<<< HEAD
services:
  schulbuddy:
    build: .
    container_name: schulbuddy-app
    ports:
      - "${EXTERNAL_PORT:-5000}:${PORT:-5000}"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=your-production-secret-key-change-this
      - DATABASE_URL=sqlite:////app/data/schulbuddy.db
      - DOCKER_ENV=1
      - HOST=${HOST:-0.0.0.0}
      - PORT=${PORT:-5000}
      - SESSION_TIMEOUT_MINUTES=120
      - REMEMBER_COOKIE_DAYS=30
      - LOGIN_TIMEOUT_MINUTES=60
      - MAX_LOGIN_ATTEMPTS=5
      - LOGIN_ATTEMPT_TIMEOUT_MINUTES=15
    volumes:
      - schulbuddy_data:/app/data
      - schulbuddy_uploads:/app/static/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:${PORT:-5000}')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: nginx reverse proxy
#  nginx:
#    image: nginx:alpine
#    container_name: schulbuddy-nginx
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
#      - ./ssl:/etc/nginx/ssl:ro
#    depends_on:
#      - schulbuddy
#    restart: unless-stopped
#    profiles:
#      - nginx
#
volumes:
  schulbuddy_data:
    driver: local
  schulbuddy_uploads:
    driver: local
  schulbuddy_data_backup:
    driver: local
=======
version: '3.8'

services:
  schulbuddy:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      - ./instance:/app/instance
      - ./static/uploads:/app/static/uploads
    environment:
      - SECRET_KEY=${SECRET_KEY:-your-secure-secret-key-here}
      - DATABASE_URL=${DATABASE_URL:-sqlite:///instance/schulbuddy.db}
      - FLASK_ENV=${FLASK_ENV:-production}
      - SESSION_TIMEOUT_MINUTES=${SESSION_TIMEOUT_MINUTES:-120}
      - CURRENT_SCHOOL_YEAR=2024/25
      - CURRENT_SEMESTER=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Optional: Nginx als Reverse Proxy
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./static:/app/static
    depends_on:
      - schulbuddy
    restart: unless-stopped
    profiles:
      - with-nginx

networks:
  default:
    name: schulbuddy-network
>>>>>>> 04627b963babc5ecf9f1ddb34bf0f7bd3421bfff
