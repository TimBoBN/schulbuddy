# SchulBuddy Docker Compose Konfiguration
# ===========================================
#
# 📥 Download dieser Datei:
# curl -o .env https://raw.githubusercontent.com/TimBoBN/schulbuddy/main/.env.example
#
# 📝 Anpassung erforderlich:
# 1. Ändere SECRET_KEY zu einem sicheren Wert
# 2. Prüfe CURRENT_SCHOOL_YEAR und CURRENT_SEMESTER
# 3. Optional: Ändere Ports und andere Einstellungen

# 🌐 Server-Einstellungen
HOST=0.0.0.0
PORT=5000
EXTERNAL_PORT=5000

# 🔐 Sicherheitseinstellungen (WICHTIG: Ändere SECRET_KEY!)
SECRET_KEY=BITTE-AENDERN-deine-sichere-geheime-schluessel-hier-einfuegen
SESSION_TIMEOUT_MINUTES=120
REMEMBER_COOKIE_DAYS=30
LOGIN_TIMEOUT_MINUTES=60
MAX_LOGIN_ATTEMPTS=5
LOGIN_ATTEMPT_TIMEOUT_MINUTES=15

# 🏫 Schuleinstellungen (an deine Schule anpassen)
CURRENT_SCHOOL_YEAR=2024/25
CURRENT_SEMESTER=1

# 🗄️ Datenbank (SQLite in Docker Volume)
DATABASE_URL=sqlite:////app/data/schulbuddy.db

# 🚀 Performance-Tuning (automatische Architektur-Erkennung)
WORKERS=auto
WORKER_CLASS=sync
TIMEOUT=120

# 🐳 Docker Image Tag (latest für Produktiv, dev für Entwicklung)
TAG=latest

# 🔧 Optionale Einstellungen
# Unkommentiere und passe bei Bedarf an:

# Alternative Image-Quellen:
# Für GitHub Container Registry:
# REGISTRY=ghcr.io/timbobn
# Für Docker Hub (Standard):
# REGISTRY=docker.io/timbobn

# Debug-Modi:
# FLASK_DEBUG=0
# LOG_LEVEL=INFO

# Erweiterte Session-Einstellungen:
# SESSION_COOKIE_SECURE=True
# SESSION_COOKIE_HTTPONLY=True
# SESSION_COOKIE_SAMESITE=Lax

# Upload-Limits:
# MAX_CONTENT_LENGTH=16777216  # 16MB in Bytes
